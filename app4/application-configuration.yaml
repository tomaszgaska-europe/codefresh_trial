apiVersion: csdp.codefresh.io/v1
kind: ApplicationConfiguration
metadata:
  name: cf-deployer
spec:
  applicationSourceSelector:
    matchLabels:
      app: app4
  priority: 2
  versionSource:
    file: service.yaml
    jsonPath: version
